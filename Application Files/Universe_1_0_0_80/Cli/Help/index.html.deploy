<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>CheckOutAddIn 本机帮助</title>
  <style>
    :root{color-scheme:light;--blue:#245b9e;--line:#d9e1ea;--bg:#f4f7fa;--text:#202830}
    *{box-sizing:border-box} body{margin:0;font:15px/1.7 "Microsoft YaHei",Segoe UI,sans-serif;color:var(--text);background:var(--bg)}
    header{background:linear-gradient(120deg,#1e4f89,#347fc2);color:#fff;padding:28px 5vw} header h1{margin:0 0 6px}
    nav{position:sticky;top:0;background:#fff;border-bottom:1px solid var(--line);padding:10px 5vw;z-index:2}
    nav a{display:inline-block;margin:3px 14px 3px 0;color:var(--blue);text-decoration:none} main{max-width:1080px;margin:auto;padding:24px}
    section{scroll-margin-top:76px;background:#fff;border:1px solid var(--line);border-radius:10px;padding:22px 26px;margin:0 0 18px;box-shadow:0 2px 8px #17324d0a}
    h2{color:var(--blue);margin-top:0;border-bottom:1px solid var(--line);padding-bottom:8px} h3{margin-bottom:3px}
    code{background:#edf2f7;padding:2px 6px;border-radius:4px} .example{border-left:4px solid #4b91cc;background:#f3f8fc;padding:10px 14px;margin:10px 0}
    .note{border-left:4px solid #d79a32;background:#fff8e8;padding:10px 14px;margin:10px 0}.danger{border-left:4px solid #c84a4a;background:#fff1f1;padding:10px 14px;margin:10px 0}
    table{width:100%;border-collapse:collapse;margin:10px 0 16px}th,td{border:1px solid var(--line);padding:8px 10px;text-align:left;vertical-align:top}th{background:#edf4fa}.syntax{font-family:Consolas,monospace;background:#17222d;color:#e8f1f8;padding:12px 14px;border-radius:6px;overflow:auto}.muted{color:#697784} footer{text-align:center;color:#697784;padding:20px}
  </style>
</head>
<body data-topic="{{ACTIVE_TOPIC}}">
<header id="overview"><h1>CheckOutAddIn 本机帮助</h1><div>随插件安装，可离线查看。点击窗口标题栏的“?”会直接定位到相应功能。</div></header>
<nav>
  <a href="#project">工程</a><a href="#check-out">导出</a><a href="#check-in">导入</a>
  <a href="#create-title">表头</a><a href="#auto-create">自动配置</a><a href="#data-call">数据调用</a>
  <a href="#version">版本</a><a href="#python">Python</a><a href="#configuration-reference">配置字段</a>
  <a href="#transform-reference">数据转换</a><a href="#troubleshooting">排错</a><a href="#log">日志</a>
</nav>
<main>
  <section><h2>开始之前：工具如何理解 Excel</h2><p>CheckOutAddIn 不是把整个工作簿当作配置，而是通过工程、版本、数据配置、表头配置和文件配置，把一个或多个 <code>ListObject</code> 组合成最终文件。理解这五层关系，可以解决绝大多数“为什么没导出”问题。</p><ol><li><b>工程</b>：以 <code>导出配置.json</code> 为根，保存全部规则。</li><li><b>版本</b>：决定本次哪些行、哪些字段生效，以及输出到哪个版本目录。</li><li><b>数据配置</b>：把业务名称映射到工作簿、Sheet、ListObject。</li><li><b>表头配置</b>：把 Excel 物理列映射为程序字段，并定义类型、默认值、转换和校验。</li><li><b>文件配置</b>：选择数据调用、格式、编码、文件名和输出路径。</li></ol><h3>ListObject 基础</h3><p>在 Excel 中选择区域后按 <code>Ctrl+T</code> 创建表，并在“表设计”中设置唯一表名。ListObject 会自动维护数据边界，工具因此不需要扫描整列。表头必须非空且不重复；重命名后应保存工作簿，再确认数据配置中的名称一致。</p><h3>有效数据行</h3><ul><li>第一列为空：读取业务数据时忽略，便于保留空编辑行。</li><li>第一列以 <code>#</code> 开头：注释行，不进入导出；字体和背景按系统配置格式化。</li><li>输入 <code>###</code>：创建三行多行注释结构；只在单单元格编辑、目标为 ListObject 数据区第一列时触发。</li><li>版本或分组不匹配：该行仍在 Excel 中，但对当前导出请求不可见。</li></ul><div class="note">配置 Sheet 的修改先同步内存，再批量保存；业务 MapData 只在确实失效后按需重载。不要通过反复关闭、打开工作簿来“刷新”。</div></section>

  <section id="project"><h2>工程管理</h2><p>解决多个游戏工程的配置隔离、登记和快速切换问题。工程以“导出配置.json”为入口，保存版本、系统、数据、表头、枚举、Python 和文件配置。</p><h3>工程列表</h3><p>右键可新建或删除。删除只移除本机登记，不删除磁盘文件。切换工程会重建活动工程上下文，不要求先打开全部 Excel。</p><h3>创建与打开</h3><ol><li>创建时输入本机唯一工程名并选择目录，工具在目录中生成导出配置.json。</li><li>已有工程直接选择原 JSON，工具只登记路径，不覆盖内容。</li><li>确认 Ribbon 中活动工程正确，再操作版本、数据和导出。</li></ol><table><tr><th>参数</th><th>说明</th><th>示例</th></tr><tr><td>工程名</td><td>本机列表和 Ribbon 显示名</td><td>巴厘之旅</td></tr><tr><td>工程目录</td><td>数据与输出相对路径的解析基准</td><td>D:\Game\Config</td></tr></table><div class="example"><b>示例：</b>登记 D:\Game\Config\导出配置.json 后，数据工作簿和输出路径都从该工程位置解析。</div></section>

  <section id="check-out"><h2>数据导出</h2><p>解决同一份 Excel 数据按版本、平台、分组、格式和路径生成不同程序文件的问题。</p><h3>执行流程</h3><ol><li>版本区可多选，默认项来自版本配置。</li><li>文件树按“父节点”分组，并根据当前工作簿预选关联文件。</li><li>先“检查配置”：读取数据、应用版本和表头规则，但不写最终文件。</li><li>再“导出所选”：执行前置 Python、格式 Writer 和后置 Python。</li><li>日志中核对每个文件的 Success/Error 和耗时。</li></ol><h3>文件参数</h3><table><tr><th>参数</th><th>作用</th></tr><tr><td>索引</td><td>文件唯一关键字和树节点名称。</td></tr><tr><td>父节点</td><td>以 # 开头的显示分组。</td></tr><tr><td>文件类型</td><td>JSON、XML、TXT、Excel。</td></tr><tr><td>通用数据调用</td><td>公共筛选，例如 行标签[服务器]。</td></tr><tr><td>关键字</td><td>数据名及行列筛选调用链。</td></tr><tr><td>文件名/后缀</td><td>最终文件名称和合法后缀。</td></tr><tr><td>编码</td><td>ASCII、UTF8、UTF8 BOM、Unicode 等。</td></tr><tr><td>文件路径</td><td>工程目录 + 版本路径 + 相对路径。</td></tr><tr><td>前置/后置 Python</td><td>单文件写入前后的校验或处理。</td></tr></table><div class="example"><b>示例：</b><code>奖励.行标签[服务器]</code> 只读取所选版本中分组包含服务器的有效行，再按表头字段、类型、默认值和转换规则输出。</div><div class="note">“导出完成”只是会话结束；必须确认每个所选文件都成功且没有错误。</div></section>

  <section id="check-in"><h2>数据导入</h2><p>导入用于把程序侧或外部文件转换为 Excel 配置表，支持单文件和文件夹批量入口。它解决的是初始化、回收线上配置和批量建表，不是用外部文件覆盖任意打开的单元格。</p><h3>单文件导入</h3><ol><li>选择源文件。文件扩展名和内容必须是工具支持的格式。</li><li>工具根据系统配置中的文件名、Sheet 名识别规则计算数据名。</li><li>确认即将创建或写入的工作簿、Sheet 和 ListObject，避免同名目标。</li><li>执行后查看日志中的数据名、行列数和目标路径，再打开结果抽查类型与中文编码。</li></ol><h3>文件夹导入</h3><p>选择文件夹和文件类型后，工具枚举符合规则的文件并逐个导入。建议先在副本目录验证；一个文件失败不应被“批量完成”信息掩盖，必须逐条检查日志。</p><table><tr><th>常见问题</th><th>处理</th></tr><tr><td>没有识别到文件</td><td>核对扩展名及系统配置中的导入文件名正则。</td></tr><tr><td>生成了错误的数据名</td><td>检查正则中的命名捕获组和工作簿后缀配置。</td></tr><tr><td>列缺失或顺序不对</td><td>先核对表头映射；导入不会凭空推断业务字段含义。</td></tr><tr><td>中文乱码</td><td>确认源编码，并在导入后抽查中文和特殊符号。</td></tr></table><div class="example"><b>示例：</b>把一批 JSON 配置导入新策划工程：先用单文件验证一个文件的名称、列和类型，再使用文件夹导入处理其余文件。</div></section>

  <section id="create-title"><h2>创建表头导出配置</h2><p>表头决定物理列是否导出、程序字段名、顺序、类型、默认值、转换和校验。创建窗口只生成骨架，不猜测业务规则。</p><ol><li>在目标 ListObject 内选择单元格。</li><li>填写唯一表头名并选择业务列；系统缺省字段会被排除。</li><li>创建后打开表头配置 Sheet 完善参数。</li></ol><table><tr><th>参数</th><th>规则</th></tr><tr><td>字段索引</td><td>Excel 物理列名，唯一；顺序即导出顺序。额外物理列是辅助列。</td></tr><tr><td>字段名</td><td>程序侧名称，必填且唯一。</td></tr><tr><td>表头</td><td>TXT/Excel 多行表头内容。</td></tr><tr><td>字段/数据类型</td><td>基础类型以及 JSON/XML 对象、数组、嵌套。</td></tr><tr><td>为空是否导出</td><td>覆盖工程级空值规则。</td></tr><tr><td>默认值</td><td>源值为空时直接输出；null 表示不允许为空。</td></tr><tr><td>分组</td><td>以 | 分隔的列标签。</td></tr><tr><td>转换类型</td><td>枚举、配置表、行内字段、数据表、Python、公式。</td></tr><tr><td>转换关键字/字段</td><td>指定转换来源、方法和目标列。</td></tr><tr><td>字段描述</td><td>表头批注和右键说明。</td></tr><tr><td>正则表达式</td><td>对非空原始值完整匹配。</td></tr></table><div class="example"><b>示例：</b>“概率”配置字段名 probability、类型 int、默认值 0、正则 <code>\d+</code>；为空输出 0，非整数文本检查失败。</div></section>

  <section id="custom-title"><h2>自定义表头</h2><p>自定义表头用于覆盖工程级 TXT/Excel 表头布局。它控制输出文件顶部有几行、每行取表头配置的哪个属性，不等于选择业务数据列。</p><ol><li>先在表头配置中完成字段索引、字段名、表头和数据类型。</li><li>在自定义表头中按输出顺序选择属性；重复选择“表头”表示依次取其用 <code>|</code> 分隔的多段内容。</li><li>在数据配置中指定 TXT 或 Excel 自定义表头；未指定时使用系统缺省布局。</li><li>导出一个小文件，核对表头行数、顺序和分隔符。</li></ol><div class="example"><b>例：</b>布局为 <code>字段名|表头|表头|字段索引</code>，某字段的“表头”为 <code>c|string</code>，则四行依次输出程序字段名、c、string、中文物理列名。</div><div class="note">自定义表头引用的属性必须真实存在。删除仍被数据配置引用的表头会被阻止。</div></section>

  <section id="auto-create"><h2>自动创建数据配置</h2><p>一次建立数据定位、表头骨架及关联，避免手工填写工作簿、Sheet、ListObject 时出错。</p><h3>前提与步骤</h3><ol><li>激活工程并保存工作簿。</li><li>在目标 ListObject 内选择单元格，不必全选。</li><li>核对数据关键字、表头名、工作簿、Sheet、ListObject 和自动索引。</li><li>创建后打开数据及表头配置，补充无法推断的业务参数。</li></ol><table><tr><th>参数</th><th>说明</th></tr><tr><td>索引</td><td>全工程调用数据的唯一关键字。</td></tr><tr><td>自动索引</td><td>TRUE 时为有效行生成内部 Key，适合不关心主键的辅助表。</td></tr><tr><td>XML 表头</td><td>每行业务数据的 XML 元素名。</td></tr><tr><td>JSON 数据类型</td><td>定义下一层对象、数组或数值结构。</td></tr><tr><td>TXT/Excel 自定义表头</td><td>覆盖系统级表头布局。</td></tr><tr><td>行内嵌套</td><td>XML 嵌套字段，以 | 分隔。</td></tr><tr><td>表头 ListObject</td><td>关联表头配置。</td></tr><tr><td>工作簿/Sheet/ListObject</td><td>业务数据三段定位。</td></tr></table><div class="example"><b>自动索引：</b>内部索引不会作为额外字段输出；表头明确声明的物理第一列仍会导出。</div><div class="note">读取时第一列为空或以 # 开头的行不进入业务数据；注释格式化只处理 # 开头，不把新建空行染色。</div></section>

  <section id="data-call"><h2>数据调用辅助输入</h2><p>用可视化选择生成 <code>数据名.方法[参数].方法[参数]</code>，避免手写筛选、连接和 JSON 嵌套。</p><table><tr><th>方法</th><th>含义</th><th>示例</th></tr><tr><td>行ID</td><td>按第一列 Key 选择，多值取并集。</td><td>奖励.行ID[1,2]</td></tr><tr><td>行标签</td><td>按业务数据“分组”筛选。</td><td>奖励.行标签[正式版]</td></tr><tr><td>列ID</td><td>按物理字段索引选择列。</td><td>奖励.列ID[概率,数量]</td></tr><tr><td>列标签</td><td>按表头“分组”筛选列。</td><td>奖励.列标签[服务器]</td></tr></table><p>行ID和行标签取并集，列ID和列标签取并集，最终行集合与列集合取交集。</p><ol><li>搜索并选择数据。</li><li>只启用需要的行列筛选并勾选候选。</li><li>按需设置键值行、列表、字典、连接或 JSON 嵌套。</li><li>设置分隔符等参数；空参数使用系统方法配置默认值。</li><li>确认后写回原单元格。</li></ol><div class="example"><b>示例：</b><code>奖池.行标签[正式版].列ID[概率]</code> 读取正式版有效行的概率列；字典连接可生成 <code>1001:2|1002:5</code>。</div><div class="note">JSON 嵌套的界面默认结构与运行时一致；转换关键字保留为备用嵌套定义。</div></section>

  <section id="input-assistance"><h2>列表辅助输入</h2><p>在配置 Sheet 或业务数据中右键“辅助输入”，工具根据当前表、列、转换类型和活动工程生成候选，避免记忆枚举名、数据名和字段名。</p><ol><li>只选中一个单元格；多选区域不会生成依赖单元格语义的菜单。</li><li>右键选择“辅助输入”。如果菜单没有出现，先确认当前单元格位于已登记配置表或可分析的业务 ListObject。</li><li>搜索、勾选一个或多个候选。窗口会按该字段要求使用 <code>|</code>、逗号或调用语法拼接。</li><li>确认写回后仍需检查组合含义；辅助输入保证语法和候选来源，不替代业务判断。</li></ol><table><tr><th>所在字段</th><th>典型候选</th></tr><tr><td>分组</td><td>客户端、服务器、平台或版本组。</td></tr><tr><td>转换关键字</td><td>枚举 ListObject、目标数据、Python 配置。</td></tr><tr><td>转换字段</td><td>目标表头中可导出的字段。</td></tr><tr><td>业务单元格</td><td>枚举策划名、目标配置 Key、数据调用表达式。</td></tr></table><div class="example"><b>示例：</b>在表头“分组”单元格勾选客户端和服务器，写回 <code>客户端|服务器</code>；在配置表转换字段中选择 <code>name</code>，表示由目标 Key 取 name 列。</div></section>

  <section id="custom-function"><h2>自定义函数输入</h2><p>公式窗口用树结构编辑工具自己的公式，不依赖 Excel 跨工作簿公式链接。公式在导出时执行，可访问当前行、工程数据和 Python。</p><ol><li>从公式列表选择函数，右侧查看用途、参数和返回值。</li><li>在参数树中选择节点；参数既可以填常量，也可以替换为另一个函数。</li><li>可变参数函数可以增加或删除参数；固定参数不能改变数量。</li><li>点击“运行”使用当前上下文预览结果。</li><li>结果正确后点击“保存公式”写回原单元格。</li></ol><div class="syntax">Vlookup(目标数据, 行Key, 字段名[, 版本])</div><div class="example"><b>示例：</b>使用 Vlookup 从“道具”数据中读取当前单元格 ID 对应的 name；目标表未打开时仍通过工程数据读取，不产生 Excel 外链。</div><div class="danger">运行预览通过不代表所有行都通过。导出前仍要执行“检查配置”，因为每行变量、版本和空值可能不同。</div></section>

  <section id="data-recombination"><h2>高级数据重组</h2><p>高级重组把更适合策划录入的横向宽表转换为程序更容易读取的纵向结构。原表不直接覆盖，结果生成到新 Sheet。</p><h3>表头模式</h3><p>奖励或档位直接写在多个表头中时，一个来源行可以展开为多条结果。勾选的关键字段会复制到每条结果；其余字段按表头逐项展开。</p><h3>分组模式</h3><p>若每 N 列是一组重复结构，设置“分组字段数”为 N。例如“道具ID、数量”每两列一组，会展开成多行的道具ID和数量。</p><h3>操作</h3><ol><li>把源区域创建为 ListObject，并在其中选择任意单元格。</li><li>从下拉框确认数据源；窗口默认选择当前区域所属表。</li><li>选择表头模式或分组模式，设置分组字段数和关键字段。</li><li>设置“为空跳过”“为0跳过”及判空字段数。</li><li>先检查预览；确认行数、关键字段和空值规则后再点“重组”。</li></ol><div class="example"><b>示例：</b>原表一行包含 <code>id,奖励1ID,奖励1数量,奖励2ID,奖励2数量</code>。分组字段数设为 2，id 设为关键字段，结果得到两行 <code>id,奖励ID,奖励数量</code>。</div></section>

  <section id="table-reset"><h2>普通表格重组</h2><p>普通重组用于改变一维数据的读取方向和写入形状，不解析业务表头。它相当于“按行或按列读取，再按指定行列数重新排版”。</p><ol><li>确认原数据预览范围。</li><li>选择按行读取或按列读取，决定源值顺序。</li><li>选择按行重组或按列重组。</li><li>填写行列数：按行插入时代表目标列数；按列插入时代表目标行数。</li><li>检查预览后执行，避免行列数填反。</li></ol><div class="example"><b>示例：</b>源数据为纵向 1～12，按行重组、行列数填 4，结果为 3 行 × 4 列。</div></section>

  <section id="version"><h2>版本配置</h2><p>用一份工程维护正式版、试玩版、地区版或平台版，并控制数据、字段和输出目录。</p><table><tr><th>参数</th><th>说明</th></tr><tr><td>版本号</td><td>参与范围比较，位数和单段长度受系统设置约束。</td></tr><tr><td>版本名称</td><td>如正式版、试玩版。</td></tr><tr><td>平台</td><td>平台或版本组。</td></tr><tr><td>版本路径</td><td>拼接在文件相对路径之前。</td></tr><tr><td>默认版本</td><td>导出窗口默认勾选项。</td></tr></table><p>配置行版本为空通常表示通用配置；具体版本行覆盖同 Key 通用行。</p><div class="example"><b>示例：</b>通用行定义默认价格，试玩版用相同 Key 增加覆盖行。正式版读取通用值，试玩版读取覆盖值。</div><div class="note">所选版本没有有效数据行时会明确报告“版本无数据”，而不是只输出空对象。</div></section>

  <section id="version-select"><h2>版本选择</h2><p>当一个配置存在多个有效版本时，选择本次查看、跳转或执行所使用的版本。</p><div class="example"><b>示例：</b>选择“试玩版”后，后续定位使用试玩版有效的数据行和表头规则。</div></section>

  <section id="python"><h2>Python 调用</h2><p>人工执行工程登记的 Python 方法，用于检查、批处理和项目自定义工具。插件使用随附的隔离 CPython，不依赖系统 Python。</p><table><tr><th>参数</th><th>说明</th></tr><tr><td>配置名</td><td>窗口显示名和调用关键字。</td></tr><tr><td>脚本路径</td><td>相对系统 Python 脚本路径解析。</td></tr><tr><td>方法名</td><td>脚本公开方法，签名通常为 execute(ctx, *parameters)。</td></tr><tr><td>参数</td><td>任意数量字符串，按顺序传入。</td></tr></table><ol><li>选择方法。</li><li>按说明填写参数。</li><li>执行并查看返回值或 traceback。</li></ol><p>脚本每次执行读取最新文件；语法错误、缺少模块、超时和越界输出都会返回结构化诊断。前后置 Python 由导出自动调用，本窗口是人工调用，两者使用相同运行时和工程 API。</p></section>

  <section id="calendar"><h2>生成日历</h2><p>将工程日历配置生成为便于策划检查的 Excel 日历。窗口支持按月份和按版本路径两种生成方式。</p><table><tr><th>模式</th><th>用途</th></tr><tr><td>按月份生成</td><td>选择月份，查看该月每天命中的活动及配置。</td></tr><tr><td>按版本路径生成</td><td>选择版本，按版本号、名称和路径生成对应日历结果。</td></tr></table><ol><li>先确保活动开始、结束日期有效，且引用的行为/条件配置存在。</li><li>选择生成类型，再选择月份或版本。</li><li>点击“生成日历”；结果按周一到周日排列，同一天的多个活动保持配置顺序。</li><li>检查跨月、同日开始结束和版本边界。</li></ol><div class="example"><b>示例：</b>国庆活动配置 10 月 1 日至 10 月 7 日，生成 10 月日历后应在七天中连续显示；同日另一个活动显示在其后。</div></section>

  <section id="map-data-shower"><h2>数据结果展示</h2><p>该窗口显示模拟、随机、Python 或数据调用产生的二维结果。它是只读结果视图，不会把结果自动写回源 ListObject。</p><ul><li>先核对结果列是否与预期字段一致。</li><li>结果为空时回到调用方检查版本、行标签、列标签和注释行，不要直接认定窗口故障。</li><li>模拟结果应同时核对样本次数、总量和概率，不只看单条记录。</li></ul><div class="example"><b>示例：</b>抽奖模拟后按奖励 ID 汇总次数；若某奖励为 0 次，检查权重、版本及前置筛选。</div></section>

  <section id="content-show"><h2>内容候选选择</h2><p>这是多个功能共用的候选窗口。标题和来源由调用场景决定，可能显示枚举值、字段、数据 Key、版本或分组。</p><ol><li>先看窗口标题，确认当前选择的是什么类型。</li><li>使用搜索缩小范围；多选时勾选顺序通常就是写回顺序。</li><li>确认后返回上级窗口；取消不会修改原单元格。</li></ol><div class="note">候选缺失通常说明目标配置尚未保存、当前版本不可见或引用类型不匹配。先修复来源配置，不要手写一个候选中不存在的名称。</div></section>

  <section id="unit-struct"><h2>单位结构</h2><p>维护兼容业务中的单位、属性和结构组合。它读取工程中的单位模板、单位对象、属性、行为和条件配置，生成引用关系。</p><ol><li>先完成被引用的属性和模板配置并关闭配置 Sheet 保存。</li><li>选择目标单位或结构，再勾选需要的属性集合。</li><li>保存后执行配置检查，确认不存在已删除的引用。</li></ol><div class="danger">这是兼容功能。删除或重命名被引用项前先搜索引用；不要直接编辑 JSON 中的关联名称。</div></section>

  <section id="configuration-reference"><h2>工程配置字段参考</h2><p>以下是最常用、最容易互相混淆的三张配置表。配置 Sheet 中看到的值会同步到内存，关闭配置页时保存到工程 JSON。</p><h3>数据配置</h3><table><tr><th>字段</th><th>含义与规则</th></tr><tr><td>索引</td><td>全工程唯一数据名，是文件关键字、转换关键字和数据调用的入口。</td></tr><tr><td>自动索引</td><td>TRUE 时工具为有效行生成内部 Key；自动列不额外导出，但表头明确声明的物理第一列仍导出。</td></tr><tr><td>版本/分组</td><td>控制配置行本身何时可见；业务数据行和字段还可以有自己的版本、分组。</td></tr><tr><td>工作簿、Sheet、ListObject</td><td>三段物理定位。工作簿使用工程相对路径或规范化路径，不要只依赖当前活动窗口。</td></tr><tr><td>表头 ListObject</td><td>指定字段映射规则；删除或改名后必须同步这里。</td></tr><tr><td>XML 表头/JSON 类型</td><td>只对对应格式生效，决定节点名和对象/数组结构。</td></tr><tr><td>TXT/Excel 自定义表头</td><td>覆盖工程缺省表头布局。</td></tr></table><h3>表头配置</h3><table><tr><th>字段</th><th>含义与规则</th></tr><tr><td>字段索引</td><td>Excel 物理列名；唯一，顺序决定输出字段顺序。</td></tr><tr><td>字段名</td><td>程序侧字段名；JSON/XML 键及字典方法会使用它。</td></tr><tr><td>是否为空列</td><td>声明占位或不从源表取值的字段。</td></tr><tr><td>表头</td><td>用 <code>|</code> 保存多段表头内容，供 TXT/Excel 布局逐段读取。</td></tr><tr><td>字段类型/数据类型</td><td>决定类型转换，以及 JSON/XML 的对象、数组、属性等结构。</td></tr><tr><td>为空是否导出/默认值</td><td>控制空字段省略与回退；默认值为 null 时通常表示不允许为空。</td></tr><tr><td>分组</td><td>字段标签；如客户端、服务器。文件的数据调用可按列标签筛选。</td></tr><tr><td>转换类型</td><td>枚举、配置表、行内字段、数据表、Python、二级转换、Json嵌套、公式。</td></tr><tr><td>转换关键字/字段</td><td>目标数据或缺省方法，以及目标字段。具体含义随转换类型变化。</td></tr><tr><td>字段描述</td><td>生成批注和右键“查看备注”，应写清单位、范围和业务含义。</td></tr><tr><td>正则表达式</td><td>对非空原始文本做完整匹配，用于导出前校验。</td></tr></table><h3>文件配置</h3><table><tr><th>字段</th><th>含义与规则</th></tr><tr><td>索引/父节点</td><td>文件唯一名与导出树分组；父节点常以 # 开头，仅用于组织显示。</td></tr><tr><td>文件类型</td><td>TXT、JSON、XML、Excel 等，决定 Writer 和必需参数。</td></tr><tr><td>通用数据调用</td><td>应用于本文件所有关键字的公共行列筛选。</td></tr><tr><td>关键字</td><td>一个或多个数据调用表达式，按配置顺序参与内容构建。</td></tr><tr><td>文件名/后缀</td><td>最终文件名；后缀应与类型及程序读取方式一致。</td></tr><tr><td>编码/分隔符</td><td>TXT 尤其重要；常用 UTF-8、UTF-8 BOM、Tab、逗号或自定义字符串。</td></tr><tr><td>文件路径</td><td>工程根 + 版本路径 + 相对路径。禁止依赖某台电脑的绝对路径，个人差异放自定义数据。</td></tr><tr><td>前置/后置 Python</td><td>前置用于校验或准备，后置用于转换或通知；任何失败都会进入该文件诊断。</td></tr></table></section>

  <section id="transform-reference"><h2>数据转换语法与示例</h2><p>转换发生在原始单元格读取之后、格式 Writer 写入之前。空值和零值是否跳过由字段及方法规则共同决定。</p><table><tr><th>类型</th><th>解决的问题</th><th>典型配置</th></tr><tr><td>枚举值</td><td>策划写中文，程序得到枚举值。</td><td>转换关键字填写枚举 ListObject；单元格写“金币”。</td></tr><tr><td>配置表</td><td>根据目标 Key 取另一张表某字段。</td><td>关键字“道具”，转换字段“name”；单元格写道具 ID。</td></tr><tr><td>行内字段</td><td>把当前行多列连接为列表或字典。</td><td><code>行内列表[1=|,2=TRUE,奖励1,奖励2]</code></td></tr><tr><td>数据表</td><td>跨数据筛选、连接、查找或生成行字典。</td><td><code>奖励.行标签[正式版].连接[1=|,2=:]</code></td></tr><tr><td>Python方法</td><td>用项目脚本计算复杂值。</td><td><code>配置名.方法名[参数1|参数2]</code></td></tr><tr><td>二级转换</td><td>依据本行另一个枚举字段决定目标枚举。</td><td>转换关键字填写父字段名。</td></tr><tr><td>Json嵌套</td><td>把另一个数据调用构造成当前字段的子对象/数组。</td><td>优先使用辅助输入生成；转换关键字可作备用结构。</td></tr><tr><td>公式</td><td>执行工具公式，避免 Excel 跨表链接。</td><td>使用公式窗口生成并预览。</td></tr></table><h3>数据表方法参数</h3><p>方法参数写在方括号中，编号参数如 <code>1=|</code>；未写的参数使用系统配置默认值。参数中的逗号需要使用工具约定的“中文逗号”表示，避免被解析为参数分隔。</p><div class="syntax">数据名.行ID[1001,1002].列ID[id,数量].连接[1=|,2=:,3=TRUE,4=TRUE,5=TRUE]</div><ul><li><b>连接</b>：二维值按列、行分隔符拼接，可选择列优先以及忽略空值。</li><li><b>行字典</b>：先形成 <code>字段名:值</code>，再拼接一行和多行。</li><li><b>查找</b>：从筛选结果定位唯一行列；目标不唯一或不存在时应报明确错误。</li><li><b>行内列表</b>：可引用未导出的物理辅助列。</li><li><b>行内字典</b>：字段名来自表头，所以只能引用已声明字段。</li></ul><div class="example"><b>完整例：</b>原表一行有“奖励1=金币、数量1=100、奖励2=钻石、数量2=5”。行内字典使用字段和值分隔符 <code>:</code>、列分隔符 <code>|</code>，可输出 <code>金币:100|钻石:5</code>。</div></section>

  <section id="troubleshooting"><h2>常见问题与排查顺序</h2><table><tr><th>现象</th><th>优先检查</th></tr><tr><td>导出提示数据没有某列</td><td>表头配置是否仍声明已删除列；关闭表头配置页确保内存提交；核对物理列名是否完全一致。</td></tr><tr><td>某版本输出空对象/无数据</td><td>该版本是否存在有效行；第一列是否为空或 # 注释；版本表达式和版本组是否匹配。</td></tr><tr><td>自动索引后第一列消失</td><td>自动索引只忽略工具自动添加的内部列；若物理第一列在表头中声明，应继续导出。</td></tr><tr><td>右键没有辅助输入</td><td>必须单选单元格；确认位于可分析 ListObject；配置表需已登记，普通业务表需有相应表头/转换上下文。</td></tr><tr><td>点击问号打开错误页面</td><td>确认插件已升级；关闭 Excel 和 Codex 后重开。帮助文件会按主题释放到本机目录。</td></tr><tr><td>修改一个单元格明显卡顿</td><td>确认不是批量公式重算；查看日志；未登记表不应触发工程解析，已登记业务表只应使对应缓存失效。</td></tr><tr><td>ListObject 已存在但提示控件重名</td><td>保存并重新打开工作簿；确认没有同名 VSTO 包装控件和原生表残留。</td></tr><tr><td>Python 找不到模块或超时</td><td>核对随附运行时、requirements、脚本路径和方法签名；查看完整 traceback。</td></tr><tr><td>TXT 中文乱码或列错位</td><td>核对编码、分隔符、自定义表头行数，以及字段值是否含未转义分隔符。</td></tr></table><h3>推荐排查顺序</h3><ol><li>在导出窗口先点“检查配置”，不要直接反复导出。</li><li>打开日志，找到第一条 Error，而不是最后一条“导出完成”。</li><li>按错误中的工程、数据名、版本、物理行、字段定位。</li><li>核对数据配置的工作簿/Sheet/ListObject，再核对表头物理列。</li><li>核对注释、空行、版本和分组是否让目标数据不可见。</li><li>修改配置 Sheet 后正常关闭页面提交，再重试。</li></ol></section>

  <section id="log"><h2>日志窗口</h2><p>日志窗口在第一次需要呈现日志时创建，之后关闭按钮只隐藏，实例在插件生命周期内复用。业务日志即使窗口隐藏也会继续记录。</p><table><tr><th>类型</th><th>颜色</th><th>含义</th></tr><tr><td>Log</td><td>白色</td><td>步骤、参数或普通进度。</td></tr><tr><td>Success</td><td>绿色</td><td>一个明确步骤成功；不代表整个批次全部成功。</td></tr><tr><td>Warning</td><td>黄色</td><td>存在异常或回退，但当前流程可能继续。</td></tr><tr><td>Error</td><td>红色</td><td>该文件、步骤或会话失败，需要处理。</td></tr></table><h3>如何读一次导出日志</h3><ol><li>找到“导出开始”，确认版本和文件。</li><li>从上往下处理第一条 Error；后续错误可能只是连锁反应。</li><li>记录数据名、版本、工作簿、Sheet、表、行、列。</li><li>即使最后出现“导出完成”，也要确认每个文件都有对应 Success。</li></ol><div class="example"><b>示例：</b>“数据表不存在列【概率】”表示当前表头仍要求物理列“概率”，但业务 ListObject 中没有同名列；应修正表头或恢复该列，而不是清空日志重试。</div></section>
</main>
<footer>CheckOutAddIn 本机帮助 · 内容随插件版本更新</footer>
<script>
(function(){var topic=document.body.getAttribute('data-topic')||'overview';var target=document.getElementById(topic)||document.getElementById('overview');if(target){document.title=target.querySelector('h1,h2').textContent+' - CheckOutAddIn';setTimeout(function(){target.scrollIntoView(true);},0);}}());
</script>
</body>
</html>
