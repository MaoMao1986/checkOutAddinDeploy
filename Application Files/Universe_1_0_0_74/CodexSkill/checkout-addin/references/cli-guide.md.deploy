# CheckOutAddIn CLI 与 AI 使用手册

> 适用对象：命令行用户、自动化脚本、AI Agent。
> 产品主线：旧版 `CheckOutAddIn` VSTO。CLI 调用的工程模型、读取、转换和导出规则均来自该主线。

## 1. 这套 CLI 能做什么

当前 CLI 提供 58 项能力，可以完成以下完整链路：

1. 创建或脚手架生成游戏工程。
2. 创建 Excel、Sheet、ListObject 和字段。
3. 批量写入、修改、删除业务数据。
4. 创建版本、枚举、表头、数据和文件导出配置。
5. 检查工程、读取业务数据、执行 Python、生成日历。
6. 预检并导出 JSON、TXT、XML、Excel 等配置文件。
7. 构建数据调用表达式、公式和各种数据重组结果。

大多数命令无需启动 Excel。只有以下能力明确依赖正在运行并加载插件的 Excel：

- `checkout import file`
- `checkout import folder`（正式导入时；`--dry-run` 不需要）
- `checkout workbook format-comments`

## 2. 找到程序并确认能力

开发构建的默认程序位置：

```text
CheckOutAddIn.Cli\bin\Debug\net472\checkout.exe
```

发布环境可以把程序目录加入 `PATH`，后续统一使用 `checkout`。

首先执行：

```powershell
checkout capabilities list
```

输出是稳定 JSON。`Value.Commands` 中每项包含：

| 字段 | 含义 |
| --- | --- |
| `Command` | 完整命令名 |
| `Category` | 功能类别 |
| `Mutates` | 是否可能修改文件或工程 |
| `RequiresExcel` | 是否要求 Excel/VSTO 桥接 |
| `Syntax` | 机器可读调用语法 |

AI 不应依赖记忆猜参数；每次使用陌生版本前，都应先读取能力目录。

## 3. 通用约定

### 3.1 工程选择器

需要工程的命令必须二选一：

```text
--project D:\GameConfig\工程.json
--project-name 已登记工程名
```

不要同时提供二者。自动化任务优先使用 `--project`，路径明确、可复现；交互环境可以使用 `--project-name`。

### 3.2 表格选择器

表格编辑支持两种定位方式。

按工程数据名：

```powershell
checkout table row add --project D:\Game\工程.json --data 道具 `
  --values '{"ID":"1001","名称":"木剑"}' --save
```

按物理工作簿：

```powershell
checkout table row add --workbook D:\Game\数据.xlsx --sheet 道具 --table Cfg_道具 `
  --values '{"ID":"1001","名称":"木剑"}' --save
```

工程数据名适合业务自动化；物理定位适合工程尚未配置完成时创建基础数据。

### 3.3 JSON 参数与 Shell 引号

数组、对象、批处理都使用 JSON。PowerShell 中建议外层使用单引号：

```powershell
--versions '["1.0.0.0","1.1.0.0"]'
--values '{"ID":"1001","名称":"木剑"}'
```

如果 JSON 很长，先写入清单文件，优先使用 `project scaffold --manifest`，不要在命令行拼接数百行 JSON。

### 3.4 返回值与退出码

标准输出永远是一个 JSON `AutomationResult`。关键字段：

| 字段 | 处理方式 |
| --- | --- |
| `Succeeded` | 只有 `true` 才算成功 |
| `ErrorCode` | `None`、`InvalidArguments`、`NotFound`、`ValidationFailed` 等 |
| `Persisted` | 是否已经真实写入 |
| `AccessKind` | `None`、`OpenXml` 或 `Interop` |
| `Diagnostics` | 稳定错误码和可读信息；失败时必须读取 |
| `OutputFiles` | 生成或涉及的文件 |
| `Value` | 命令特有结构化结果 |

AI 应同时检查进程退出码和 `Succeeded`，不能只根据是否产生文本判断成功。

### 3.5 `--dry-run`、`--save` 与预检

- 支持 `--dry-run` 的命令：完整校验请求，但不落盘。
- 表格编辑中的 `--save`：对关闭的工作簿应显式提供，表示持久化修改。
- `export validate` / `batch-validate`：执行导出预检，不写导出产物。
- `worksheet format` 不带 `--save` 时是预检；带 `--save` 才修改关闭的工作簿。

推荐顺序：预检 → 检查结构化结果 → 正式执行 → 再次查询验证。

## 4. 推荐给 AI 的完整工程工作流

### 4.1 首选：一次脚手架生成

创建 `game-manifest.json`：

```json
{
  "name": "示例游戏",
  "directory": "D:\\GameConfig\\示例游戏",
  "projectFile": "工程.json",
  "register": true,
  "setActive": true,
  "platforms": ["客户端"],
  "versions": [
    {
      "code": "1.0.0.0",
      "name": "正式版",
      "path": "D:\\GameBuild\\正式版",
      "platform": "客户端",
      "default": true
    }
  ],
  "workbooks": [
    {
      "file": "数据.xlsx",
      "sheets": [
        {
          "name": "道具",
          "tables": [
            {
              "name": "Cfg_道具",
              "start": "A1",
              "columns": ["ID", "名称", "品质", "版本"]
            }
          ]
        }
      ]
    }
  ],
  "titles": [],
  "enums": [
    {
      "name": "Enum_品质",
      "rows": [
        { "枚举名称": "普通", "枚举值": 0 },
        { "枚举名称": "稀有", "枚举值": 1 }
      ]
    }
  ],
  "data": [
    {
      "name": "道具",
      "workbook": "数据.xlsx",
      "sheet": "道具",
      "table": "Cfg_道具",
      "configuration": true,
      "autoIndex": true,
      "createTitle": true
    }
  ]
}
```

先预检，再创建：

```powershell
checkout project scaffold --manifest D:\Temp\game-manifest.json --dry-run
checkout project scaffold --manifest D:\Temp\game-manifest.json
checkout project validate --project D:\GameConfig\示例游戏\工程.json
```

脚手架创建在临时目录中，全部成功后才切换为最终目录；失败不应留下半成品工程。

### 4.2 写入第一批业务数据

批量写入应优先于逐单元格调用：

```powershell
checkout table batch --project D:\GameConfig\示例游戏\工程.json --data 道具 --save `
  --operations '[
    {"Kind":"AddRow","Values":{"ID":"1001","名称":"木剑","品质":"普通","版本":"正式版"}},
    {"Kind":"AddRow","Values":{"ID":"1002","名称":"铁剑","品质":"稀有","版本":"正式版"}}
  ]'
```

随后验证：

```powershell
checkout data read --project D:\GameConfig\示例游戏\工程.json --name 道具 `
  --version 1.0.0.0 --columns '["ID","名称","品质"]'
```

### 4.3 配置和导出

```powershell
checkout project validate --project D:\GameConfig\示例游戏\工程.json
checkout export batch-validate --project D:\GameConfig\示例游戏\工程.json `
  --versions '["1.0.0.0"]' --files '["道具"]'
checkout export batch --project D:\GameConfig\示例游戏\工程.json `
  --versions '["1.0.0.0"]' --files '["道具"]'
```

不要跳过 `project validate` 和导出预检。工程结构有效不代表某个版本一定有可导出数据。

## 5. 58 项接口总览

### 5.1 工程（7）

| 命令 | 用途 |
| --- | --- |
| `checkout project create` | 创建空的旧版工程 JSON |
| `checkout project scaffold` | 从清单原子创建工程、工作簿、表、版本、枚举和数据配置 |
| `checkout project inspect` | 查询版本、配置 Schema 和配置行 |
| `checkout project validate` | 检查工程引用、工作簿和 ListObject |
| `checkout project list` | 查询登记工程及活动工程 |
| `checkout project register` | 登记已有工程，可设为活动工程 |
| `checkout project unregister` | 仅取消登记，不删除工程文件 |

### 5.2 版本（6）

`checkout version add`、`checkout version update`、`checkout version remove`、`checkout version default-toggle`、`checkout version platform-add`、`checkout version platform-remove`。

`code` 是工程内部版本号；数据中的“正式版/试玩版”等版本名称仍按旧版工程规则解析，不要自行把名称和版本号视为相同字段。

### 5.3 表头和枚举（6）

| 命令 | 用途 |
| --- | --- |
| `checkout title create` | 创建表头配置；不会重新加入版本、分组等系统缺省字段 |
| `checkout title delete` | 删除未被数据配置引用的表头 |
| `checkout title field-add` | 给一个或多个表头增加字段 |
| `checkout title field-remove` | 删除字段并立即刷新工程配置 |
| `checkout enum create` | 创建自定义枚举及枚举行 |
| `checkout enum delete` | 删除未被转换配置引用的枚举 |

### 5.4 数据配置和读取（6）

| 命令 | 用途 |
| --- | --- |
| `checkout data configure` | 用明确列清单创建数据/文件/可选表头配置 |
| `checkout data configure-table` | 从实际 ListObject 列创建配置 |
| `checkout data remove` | 删除数据、文件及不再引用的关联表头配置 |
| `checkout data read` | 按旧版版本、Key、列和分页规则读取业务数据 |
| `checkout data-call build` | 构建筛选、Json 嵌套和连接表达式 |
| `checkout data-call parse` | 把已有数据调用表达式回填成结构化状态 |

数据调用示例：

```powershell
checkout data-call build --input '{
  "DataName":"奖励",
  "RowIds":["1001"],
  "ColumnIds":["ID","数量"],
  "Json":{"Outer":"数组","Inner":"对象","Key":"ID"},
  "Connection":{"Name":"查找","Overrides":{"1":"ID"}}
}'
```

输出：

```text
奖励.行ID[1001].列ID[ID,数量].嵌套结构[数组|对象|ID].查找[1=ID]
```

### 5.5 工作簿结构和读取（8）

`checkout workbook create`、`checkout workbook inspect`、`checkout workbook format-comments`、`checkout worksheet format`、`checkout sheet add`、`checkout sheet delete`、`checkout listobject create`、`checkout listobject delete`。

技巧：

- 对关闭工作簿的结构编辑使用 OpenXML，不需要启动 Excel。
- `format-comments` 需要 Excel，因为批注、验证和活动插件上下文属于 VSTO 行为。
- 创建 ListObject 时列名必须唯一；`--start` 缺省为 `A1`。

### 5.6 表格编辑（6）

`checkout table row add`、`checkout table row delete`、`checkout table column add`、`checkout table column delete`、`checkout table cell set`、`checkout table batch`。

批处理操作结构：

```json
[
  { "Kind": "AddRow", "Values": { "ID": "1001", "名称": "木剑" } },
  { "Kind": "SetCell", "Key": "1001", "Column": "名称", "Value": "精制木剑" },
  { "Kind": "AddColumn", "Column": "描述", "Value": "", "Index": 2 },
  { "Kind": "DeleteColumn", "Column": "废弃字段" },
  { "Kind": "DeleteRow", "Key": "1002" }
]
```

一次 `batch` 是一个原子事务：中间任一操作失败，原文件保持不变。

### 5.7 通用工程配置（1）

```text
checkout config batch
```

操作对象：

```json
{
  "Target": "calendar",
  "Configuration": null,
  "Kind": "Add",
  "Key": null,
  "Version": "",
  "Values": { "关键字": "活动", "数据关键字": "活动数据" }
}
```

支持的 `Target`：

```text
data, file, title, enum, python, property, unitobject, unittemplate,
condition, behaviour/behavior, enumfile, calendar, system,
customdata, customdatafield
```

`title` 和 `enum` 必须额外提供 `Configuration`。`Kind` 为 `Add`、`Update` 或 `Delete`。整个数组一次提交，失败整体回滚。

### 5.8 导入导出（6）

| 命令 | 用途 |
| --- | --- |
| `checkout import file` | 通过 Excel 插件导入单个文件 |
| `checkout import folder` | 按系统后缀表导入目录第一层文件 |
| `checkout export file` | 导出单个版本/文件配置 |
| `checkout export validate` | 单文件预检 |
| `checkout export batch` | 一个会话导出多个版本和文件 |
| `checkout export batch-validate` | 批量预检 |

文件夹导入支持 `xml`、`json`、`txt`、`excel`。先运行：

```powershell
checkout import folder --project D:\Game\工程.json --folder D:\Import --type json --dry-run
```

确认 `Value.Files` 后再去掉 `--dry-run`。

### 5.9 数据处理和公式（6）

| 命令 | 用途 |
| --- | --- |
| `checkout transform combination` | 多列笛卡尔积 |
| `checkout transform repeat` | 按数量重复二列表格 |
| `checkout transform reset` | 按行/列顺序重排矩阵 |
| `checkout transform recombine` | 表头模式或分组模式高级重组 |
| `checkout formula functions` | 返回旧版自定义函数目录 |
| `checkout formula evaluate` | 使用旧版解析器求值 |

公式必须以 `~` 开头：

```powershell
checkout formula evaluate --expression '~(score+3)*2' --variables '{"score":4}'
```

### 5.10 日历、模拟和坐标（4）

| 命令 | 用途 |
| --- | --- |
| `checkout calendar layout` | 用显式事件生成月份矩阵与颜色计划 |
| `checkout calendar generate` | 读取工程日历配置和业务数据，生成真实 Excel |
| `checkout simulation inspect` | 按旧版对象/模板/属性配置构建只读模拟树 |
| `checkout coordinate generate` | 从内容和颜色矩阵生成坐标数据 |

完整日历生成：

```powershell
checkout calendar generate --project D:\Game\工程.json `
  --versions '["1.0.0.0"]' `
  --months '[{"year":2026,"month":9},{"year":2026,"month":10}]' `
  --mode version --output D:\Game\Calendar --dry-run
```

`--mode version`：每个版本一个工作簿；`--mode month`：每个月一个工作簿、版本作为 Sheet。

### 5.11 Python 与能力发现（2）

| 命令 | 用途 |
| --- | --- |
| `checkout python run` | 按工程 Python 配置名或脚本路径调用方法 |
| `checkout capabilities list` | 返回本机当前 CLI 的全部接口契约 |

Python 示例：

```powershell
checkout python run --project D:\Game\工程.json --script tools\generate.py `
  --method execute --parameters '["正式版","客户端"]' --timeout 30000
```

## 6. AI 使用技巧

### 6.1 先查询，再修改

推荐决策顺序：

1. `capabilities list`：确认当前安装版本支持什么。
2. `project inspect --include-rows`：理解工程配置及 Schema。
3. `workbook inspect`：确认 Sheet、ListObject、列和现有行。
4. 使用 `--dry-run` 或 `validate`。
5. 执行写入。
6. 再次 `inspect` / `read` / `validate` 验证后置条件。

### 6.2 优先使用高层命令

- 新工程：优先 `project scaffold`，不要手工拼完整工程 JSON。
- 已有 ListObject 建配置：优先 `data configure-table`。
- 多行数据：优先 `table batch`。
- 多版本导出：优先 `export batch-validate` + `export batch`。
- 任意工程配置批量修改：使用一次 `config batch`，不要逐项反复保存。

### 6.3 不要绕开旧版业务语义

- 不要直接编辑工程 JSON 来替代版本、表头、枚举和配置命令。
- 不要自行实现版本过滤、自动索引、注释行或字段转换。
- 不要把 Universe 工程当作产品入口。
- 数据 ListObject 第一列以 `#` 开头或为空的行，读取时按现有旧版规则作为注释/无效数据处理。
- 自动索引是否导出、系统字段过滤、Json 嵌套优先级均由旧版运行逻辑决定。

### 6.4 控制变更范围

- 写入前记录目标工程、工作簿、Sheet、ListObject 和 Key。
- 不要用模糊文件名猜目标。
- 对删除操作先查询引用关系。
- 不要把同目录下不属于工程的文件隐式纳入操作。
- 返回 `Persisted=false` 时，不要宣称文件已修改。

### 6.5 处理失败

失败时应把 `Diagnostics[].Code` 作为机器判断依据，把 `Message` 提供给用户。不要在参数错误、配置错误或缺少 Excel 桥接时无条件重复执行。

常见类别：

| 情况 | 建议 |
| --- | --- |
| `InvalidArguments` | 修正参数和 JSON，不要重试原命令 |
| `NotFound` | 重新 inspect，确认工程、数据、Sheet、表或 Key |
| `ValidationFailed` | 修复工程配置或数据内容后再次预检 |
| `WorkbookConflict` | 确认工作簿是否在 Excel 中打开及桥接状态 |
| `BridgeUnavailable` / `UnsupportedInHeadlessMode` | 启动 Excel 并加载插件，或改用无需 Excel 的命令 |

## 7. 给其他 AI 的建议提示词

可以把下面内容作为其他 AI 的系统/任务说明：

```text
你通过 CheckOutAddIn CLI 操作游戏配置工程。开始前运行
`checkout capabilities list`，以返回的 Command、Syntax、Mutates 和
RequiresExcel 为唯一接口目录。

执行规则：
1. 使用 --project 明确指定工程，除非用户明确要求使用已登记工程名。
2. 修改前先 project inspect/workbook inspect；支持 dry-run 或 validate 时先预检。
3. 批量数据优先 table batch，工程级创建优先 project scaffold。
4. 每次解析 AutomationResult：只有 Succeeded=true 才继续；Persisted 表示是否已落盘。
5. 失败时报告 Diagnostics.Code 和 Message，不要盲目重试。
6. 修改后用 inspect/read/validate 验证结果。
7. 不直接改工程 JSON，不重写版本筛选、自动索引、注释行和转换业务规则。
8. RequiresExcel=true 时先确认 Excel 已启动并加载 CheckOutAddIn。
```

## 8. 快速核对清单

交付 AI 自动化结果前，至少确认：

- [ ] 使用的是旧版 `CheckOutAddIn` 主线 CLI。
- [ ] 能力目录来自当前机器，而不是历史记忆。
- [ ] 路径、工程、版本、数据名和表定位明确。
- [ ] 已执行可用的 `--dry-run` / `validate`。
- [ ] 所有命令均返回 `Succeeded=true`。
- [ ] 需要持久化的操作返回 `Persisted=true`。
- [ ] 写入后通过查询或校验确认结果。
- [ ] 没有隐式修改工程范围之外的文件。
