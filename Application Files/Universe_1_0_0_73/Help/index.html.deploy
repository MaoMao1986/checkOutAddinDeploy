<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CheckOutAddIn 帮助</title><style>
body{margin:0;font:15px/1.75 "Microsoft YaHei";color:#202830;background:#f3f6f9}header{padding:24px 5%;color:white;background:#245b9e}.layout{display:grid;grid-template-columns:230px minmax(0,900px);gap:20px;max-width:1200px;margin:auto;padding:20px}nav,.page{background:white;border:1px solid #d8e1ea;border-radius:9px;padding:18px}nav{height:max-content;position:sticky;top:15px}nav a{display:block;padding:4px 8px;color:#245b9e;text-decoration:none}.page{display:none}.page.active{display:block}h2{color:#245b9e;border-bottom:1px solid #d8e1ea;padding-bottom:8px}.note,.example{padding:9px 13px;background:#fff7df;border-left:4px solid #d39a2f}.example{background:#f1f7fc;border-color:#4b91cc}code{background:#edf2f7;padding:2px 5px}@media(max-width:700px){.layout{display:block}nav{position:static;margin-bottom:15px}}
</style></head><body><header><h1>CheckOutAddIn 本机帮助</h1><div>标题栏“?”会直接打开当前功能页。</div></header><div class="layout"><nav id="nav"></nav><main>
<section class="page" id="overview" data-title="使用总览"><h2>使用总览</h2><p>工具把策划可读的 Excel ListObject 按工程配置转换为 JSON、XML、TXT 或 Excel，解决版本差异、枚举记忆、跨表 ID、重复录入和大量公式计算问题。</p><ol><li>打开工程并确认活动工程。</li><li>建立版本以及数据、表头、文件配置。</li><li>维护业务 ListObject。</li><li>在导出窗口选择版本和文件，并检查日志。</li></ol><p class="note">具体版本配置覆盖通用配置；第一列为空或以 # 开头的行读取时作为注释忽略。</p></section>
<section class="page" id="project" data-title="工程管理"><h2>工程管理</h2><p>工程保存数据表、表头、枚举、版本、输出文件和 Python 配置的关系，所有查找和导出均以活动工程为作用域。</p><ol><li>创建或选择工程配置文件。</li><li>检查工程目录和关联工作簿路径。</li><li>确认后切换活动工程，再执行配置或导出。</li></ol><p>表定位由工作簿、Sheet、ListObject 名共同决定；工作簿打开时走 Excel，未打开时才读取磁盘文件。</p></section>
<section class="page" id="version" data-title="版本管理"><h2>版本管理</h2><p>版本用于让同一份数据按正式版、试玩版、渠道或平台导出不同字段与路径。版本为空是通用配置，具体版本覆盖通用项；默认版本只决定窗口初始勾选。</p><p class="example">通用行定义文件名，试玩版行只改路径：导出试玩版时继承文件名并使用试玩版路径。若该版本没有有效数据，会明确提示“版本无数据”。</p></section>
<section class="page" id="version-select" data-title="版本选择"><h2>版本选择</h2><p>选择本次查看、定位或执行所用版本。允许多版本的操作可多选；只允许单版本的操作会拒绝多选。解析时先找具体版本，再回退通用配置。</p></section>
<section class="page" id="check-out" data-title="数据导出"><h2>数据导出</h2><p>窗口根据文件导出配置把所选文件输出到各版本目录。打开时会结合已打开工作簿自动勾选相关文件。</p><ol><li>选择一个或多个版本。</li><li>选择文件并执行“检查配置”。</li><li>点击导出，在日志中逐文件核对结果。</li><li>“生成文件到工程”会调用系统配置的方法，通常只允许一个版本。</li></ol><p>注释行不导出；自动索引仅提供内部索引，不会吞掉表头明确声明的第一列；关闭配置 Sheet 后缓存失效，下次导出按需读取最新配置。</p></section>
<section class="page" id="check-in" data-title="数据导入"><h2>数据导入</h2><p>把单文件或文件夹中的外部配置解析到 Excel。选择源格式，核对目标工作簿、Sheet、ListObject 和表头映射，再执行并检查未识别字段、类型错误及重复表名。首次导入新格式建议先复制工作簿验证。</p></section>
<section class="page" id="auto-create" data-title="创建数据配置"><h2>根据数据创建配置</h2><ol><li>在目标 ListObject 内选择单元格。</li><li>工具确认表位置并枚举物理列。</li><li>填写数据名及自动索引设置。</li><li>创建后补充字段类型、默认值、分组和转换。</li></ol><p class="note">系统缺省字段不会作为业务列重复创建；自动索引也不等于删除物理第一列。</p></section>
<section class="page" id="create-title" data-title="创建表头"><h2>创建表头配置</h2><p>根据所选 ListObject 的业务列建立表头结构。创建后应填写：字段索引（Excel 列）、字段名（输出名）、字段/数据类型、默认值、为空是否导出、分组、转换类型、转换关键字、转换字段和正则表达式。</p></section>
<section class="page" id="custom-title" data-title="自定义表头"><h2>自定义表头</h2><p>指定 TXT、Excel 等文件前几行使用的字段及顺序。候选来自表头配置；单文件设置可覆盖系统默认值。</p><p class="example">TXT 要求 id、name、probability 时，应按该顺序选择，而非数据表物理列顺序。</p></section>
<section class="page" id="data-call" data-title="数据调用"><h2>数据调用辅助输入</h2><p>通过选择生成数据表达式，筛选结果始终是二维数据块。支持：</p><ul><li><code>技能.行ID[1,2]</code>：匹配第一列。</li><li><code>技能.行标签[正式版]</code>：匹配行分组。</li><li><code>技能.列ID[note]</code>：选择字段。</li><li><code>技能.列标签[客户端]</code>：匹配列分组。</li></ul><p>条件可组合，再使用连接、查找、行字典、Json 嵌套或数据连接。表头“转换关键字”是缺省方法，单元格显式写法优先。</p></section>
<section class="page" id="input-assistance" data-title="辅助输入"><h2>列表辅助输入</h2><p>对枚举、数据 ID、字段等有限候选进行搜索和多选，并按字段约定的分隔符回填。配置表转换还支持 ID 跳转，直接打开被引用表并定位到对应行。</p></section>
<section class="page" id="custom-function" data-title="自定义函数"><h2>自定义函数输入</h2><p>选择工具支持的方法，查看系统“方法配置”中的参数顺序和默认值，填写需要覆盖的参数，预览表达式后写回。缺省参数可以省略，单行特殊参数可显式覆盖。</p></section>
<section class="page" id="data-recombination" data-title="高级重组"><h2>高级数据重组</h2><p>把横向奖励结构展开为程序所需的多行结构。表头模式把多个奖励列逐项展开；分组模式把“物品ID、数量”等固定宽度列组逐组展开。关键字段复制到每个结果行。</p><ol><li>选择源 ListObject 和模式。</li><li>设置关键字段、奖励列或组宽。</li><li>先预览行数和结果。</li><li>确认后在新 Sheet 生成。</li></ol></section>
<section class="page" id="table-reset" data-title="表格重组"><h2>表格重组设置</h2><p>用于普通行列重排、转置或组合。设置数据源、关键列、方向、起始位置和是否保留表头后执行。先预览，避免辅助列被当成业务列。</p></section>
<section class="page" id="python" data-title="Python 调用"><h2>Python 人工调用</h2><p>选择工程 Python 方法并按定义顺序填写参数。插件使用安装包内的隔离 CPython，不依赖目标电脑另装 Python。执行结果和 traceback 写入日志；列表、字典参数建议用辅助输入生成。</p></section>
<section class="page" id="calendar" data-title="日历模拟"><h2>日历模拟</h2><p>选择日期范围和日历配置，模拟每天触发的活动、奖励或状态。模拟只用于检查配置，不会替代原始数据。</p></section>
<section class="page" id="map-data-shower" data-title="结果展示"><h2>数据结果展示</h2><p>展示模拟、随机或数据调用结果，用于核对命中行、输出列和次数，不修改原数据。结果为空时检查版本、标签、注释行和筛选条件。</p></section>
<section class="page" id="content-show" data-title="候选选择"><h2>内容候选选择</h2><p>搜索字段、枚举等候选；按调用方要求单选或多选，确认后按显示顺序回填。搜索仅过滤显示，不修改源数据。</p></section>
<section class="page" id="unit-struct" data-title="单位结构"><h2>单位结构</h2><p>维护兼容性的单位或结构配置。不了解工程既有结构时不要批量修改，操作前建议备份工程配置。</p></section>
<section class="page" id="log" data-title="日志"><h2>日志窗口</h2><p>按时间显示 Log、Success、Warning 和 Error。关闭只隐藏窗口，后续日志仍写入原实例。</p><ol><li>先找第一条 Error，不要只看最后的“导出完成”。</li><li>记录数据名、版本、文件、行和列。</li><li>“不存在列”表示表头仍要求字段；“版本无数据”表示筛选后没有有效业务行。</li><li>Python 错误查看 traceback 的首个业务脚本位置。</li></ol></section>
</main></div><script>(function(){var ps=[].slice.call(document.querySelectorAll('.page')),nav=document.getElementById('nav');ps.forEach(function(p){var a=document.createElement('a');a.href='#'+p.id;a.textContent=p.dataset.title;nav.appendChild(a)});function show(){var id=decodeURIComponent(location.hash.slice(1)||'overview'),p=document.getElementById(id);if(!p||!p.classList.contains('page'))p=document.getElementById('overview');ps.forEach(function(x){x.classList.toggle('active',x===p)});document.title=p.dataset.title+' - CheckOutAddIn 帮助';scrollTo(0,0)}addEventListener('hashchange',show);show()})();</script></body></html>
